<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <!--        <item name="android:windowFullscreen">true</item>-->
        <!--        <item name="android:windowContentOverlay">@null</item>-->
        <item name="android:windowBackground">@drawable/splash_drawable</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
     </style>

    <!-- Custom Text Appearance Heavy-->
    <style name="CustomTextAppearance.Heavy" parent="android:TextAppearance">
        <item name="fontPath">font/Avenir-Heavy.ttf</item>
    </style>

    <!-- Custom Text Appearance Book-->
    <style name="CustomTextAppearance.Book" parent="android:TextAppearance">
        <item name="fontPath">font/Avenir-Book.ttf</item>
    </style>

    <!-- Custom Text Appearance Black-->
    <style name="CustomTextAppearance.Black" parent="android:TextAppearance">
        <item name="fontPath">font/Avenir-Black.ttf</item>
    </style>

    <style name="CustomTextAppearance.TAB" parent="android:TextAppearance">
        <item name="fontPath">font/Avenir-Book.ttf</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="progress_dialog_theme" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowIsFloating">false</item>
    </style>

</resources>
